# 2026-05-15 (금)

## webmail-watch-kirams cron 첫 발화 — 차단

08:59 KST cron `webmail-watch-kirams` 첫 발화에서 부트스트랩 의존성 부족으로 실패.

### 진행 상황

- `uv sync` 자동 수행 — venv 생성 + pyotp/playwright 설치 OK
- `uv run playwright install chromium` — chromium-headless-shell v1217 다운로드 OK
- **차단점**: chrome-headless-shell 실행 시 `libnspr4.so` 누락. system 공유 라이브러리 필요.

### Dr. Ben 액션 필요 (sudo 암호 필요)

```bash
cd ~/.openclaw/workspace/skills/webmail-watch && uv run playwright install-deps chromium
```

내부적으로 `sudo apt-get install -y libasound2t64 libatk-bridge2.0-0t64 libatk1.0-0t64 libatspi2.0-0t64 libcairo2 libcups2t64 libdbus-1-3 libdrm2 libgbm1 libglib2.0-0t64 libnspr4 libnss3 libpango-1.0-0 libx11-6 libxcb1 libxcomposite1 libxdamage1 libxext6 libxfixes3 libxkbcommon0 libxrandr2 xvfb fonts-noto-color-emoji fonts-unifont libfontconfig1 libfreetype6 xfonts-cyrillic xfonts-scalable fonts-liberation fonts-ipafont-gothic fonts-wqy-zenhei fonts-tlwg-loma-otf fonts-freefont-ttf` 실행.

### 그 다음 — bootstrap (headed 1회)

```bash
cd ~/.openclaw/workspace/skills/webmail-watch && uv run run.py kirams --bootstrap
```

신규 chrome-profile 이라 "아이디저장" 쿠키 + 세션 쿠키 영속화 필요. WSLg 떠 있어야 함.

이후 cron 다음 회차 (매 30분 평일 08~17시) 부터 정상 polling.

### 관찰

- skill SKILL.md 의 "최초 설치" 절차는 sudo 단계가 명시되지 않음. `uv sync` + `playwright install chromium` 두 명령만 안내. 다음 SKILL.md 업데이트 시 `playwright install-deps` 도 명시하면 신환경 부트스트랩이 매끄러울 듯.
